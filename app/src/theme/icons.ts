/**
 * Single re-export point for every lucide icon used in the app -- keeps icon
 * choices consistent across features and avoids each screen importing directly
 * from lucide-react-native. Default usage: size={20}, strokeWidth={1.75},
 * color from theme/tokens.ts.
 */
export {
  Home,
  RotateCw,
  Scale,
  Users,
  CheckCircle2,
  AlertTriangle,
  ArrowLeftRight,
  Repeat2,
  Clock,
  // Phase 4 additions: baseline photo capture (onboarding), feedback retract
  // action, market composer bottom sheet.
  Camera,
  Lock,
  Trash2,
  X,
  Plus,
  PlusCircle,
  MinusCircle,
  XCircle,
} from 'lucide-react-native';
