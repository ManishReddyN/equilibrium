# equilibrium

Household chore platform: native iOS + Android app (React Native CLI bare workflow) + Supabase/PostgreSQL backend + Fastlane CI/CD.

Execution plan and progress: see `docs/DECISIONS.md`, `docs/ARCHITECTURE.md`, `docs/RUNBOOK.md`. Full bootstrap instructions land in Phase 7.

Status: Phase 0 complete (repo scaffold, RN 0.76.5 bare workflow app, strict TypeScript, lint gates). See `docs/RUNBOOK.md` for environment blockers (iOS requires macOS/Xcode; Android currently blocked on missing JDK/Android SDK).
